export default [
  // remove ???
  // [
  //   /^text-(.*)$/,
  //   ([, c], { theme }) => {
  //     if (theme.colors[c]) return { color: `rgba(${theme.colors[c]}, 1)` }
  //   }
  // ],
  // [
  //   /^text-(.*)?:(.+)$/,
  //   ([, c, d], { theme }) => {
  //     d = d || 100
  //     if (theme.colors[c]) return { color: `rgba(${theme.colors[c]}, ${d / 100})` }
  //   }
  // ],
  // [
  //   /^bg-(.*)$/,
  //   ([, c], { theme }) => {
  //     if (theme.colors[c]) return { 'background-color': `rgba(${theme.colors[c]}, 1)` }
  //   }
  // ],
  // [
  //   /^bg-(.*)?:(.+)$/,
  //   ([, c, d], { theme }) => {
  //     d = d || 100
  //     if (theme.colors[c]) return { 'background-color': `rgba(${theme.colors[c]}, ${d / 100})` }
  //   }
  // ],
  // [
  //   /^border-(.*)$/,
  //   ([, c], { theme }) => {
  //     if (theme.colors[c]) return { 'border-color': `rgba(${theme.colors[c]}, 1)` }
  //   }
  // ],
  // [
  //   /^border-(.*)?:(.*)$/,
  //   ([, c, d], { theme }) => {
  //     d = d || 100
  //     if (theme.colors[c]) return { 'border-color': `rgba(${theme.colors[c]}, ${d / 100})` }
  //   }
  // ]
]
